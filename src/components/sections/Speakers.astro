---
import { SpeakersList } from '../../features/home/components/SpeakersList';
---

<section id="ponentes" class="relative py-24 bg-dark/30 overflow-hidden">
  <!-- Glowing forest ambient backdrop -->
  <div
    class="absolute top-1/4 right-0 w-96 h-96 bg-primary/5 rounded-full blur-3xl pointer-events-none"
  >
  </div>
  <div
    class="absolute bottom-1/4 left-0 w-96 h-96 bg-secondary/5 rounded-full blur-3xl pointer-events-none"
  >
  </div>

  <div class="container mx-auto px-6 md:px-12 relative z-10">
    <!-- Section Header -->
    <div class="text-center mx-auto mb-16 flex flex-col gap-4">
      <span class="text-xs font-bold text-secondary uppercase tracking-widest">
        Ponentes e Invitados
      </span>
      <h2 class="font-display font-extrabold text-3xl sm:text-4xl md:text-5xl text-light">
        Investigadores y Líderes Académicos
      </h2>
      <p class="text-sm sm:text-base text-light/75 leading-relaxed">
        Contamos con la participación de destacados científicos de nivel internacional,
        especialistas en conservación, medicina tropical, ciencia de datos y gobernanza territorial.
      </p>
    </div>

    <!-- Speakers Grid (Dynamically loaded from Database) -->
    <SpeakersList client:visible />
  </div>
</section>
